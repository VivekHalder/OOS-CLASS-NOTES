to resolve name conflicts======>

if two classes have the same name in different packages, 
import one class and use fully qualified path name for the other while instantiating it.

else use fully qualified path name for both.